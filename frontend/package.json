{
  "name": "flashmkt-frontend",
  "private": true,
  "version": "1.0.0",
  "description": "FLASH//MKT frontend monorepo: web app + extractable microfrontend packages",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace apps/web",
    "build": "npm run build --workspace apps/web",
    "preview": "npm run preview --workspace apps/web",
    "lint": "eslint apps packages"
  },
  "type": "module"
}
